// https://leetcode.com/problems/couples-holding-hands

class Solution {
public:
    int minSwapsCouples(vector<int>& row) {
        // graph question 
        // disjoint set union!!!
        // figure out length of the chains -> sum(chain_length - 1)
        return 0;
    }
};